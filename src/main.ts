import './styles.css';

// Boot placeholder — replaced in Task 3 with the real shell.
const app = document.getElementById('app')!;
app.innerHTML =
  '<div data-theme="dark" style="position:fixed;inset:0;display:flex;align-items:center;' +
  'justify-content:center;background:var(--bg);color:var(--text);font-family:Sora,sans-serif">' +
  'boot ok</div>';
